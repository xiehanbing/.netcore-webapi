using log4net;

namespace General.Log
{
    public interface ILogManager
    {
        void Info(object message, LogManagerType logManagerType = LogManagerType.File);

        void Debug(object message);

        void Error(object message);
    }
}